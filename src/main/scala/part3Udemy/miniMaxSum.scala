package part3Udemy

object miniMaxSum extends App{

   /* val arr=Array(1,2,3,4,5)
    var max=0
    var min=0
    var sum1=0
    var sum2=0
    for(i <- Range(0, arr.length)
    {
      if(arr(i)>max) max=arr(i)
      if(arr(i)<min) min=arr(i)
    }
      for(i <- Range(0, arr.length)
      {
        if(arr[i] max) sum1 +=arr[i]
        if(arr[i] =! min) sum2+=arr[i]

      }*/
}




